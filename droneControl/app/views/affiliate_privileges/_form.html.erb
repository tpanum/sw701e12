<%= form_for(@affiliate_privilege) do |f| %>
  <% if @affiliate_privilege.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliate_privilege.errors.count, "error") %> prohibited this affiliate_privilege from being saved:</h2>

      <ul>
      <% @affiliate_privilege.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
