<% @page_title = "Role List" %>

<div class="role list">
  <h2>Roles</h2>

  <%= link_to("<< Back to Menu", {:controller => 'admin'}, :class => 'back-link') %><br />
  <%= link_to("Add New Role", {:action => 'new'}, :class => 'action new') %>

  <table class="listing" summary="Role list">
    <tr class="header">
      <th>Id</th>
      <th>Title</th>
      <th>Actions</th>
    </tr>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.id %></td>
        <td><%= role.title %></td>
        <td class="actions">
          <%= link_to("", {:action => 'show', :id => role.id}, :class => 'show') %>
          <%= link_to("", {:action => 'edit', :id => role.id}, :class => 'edit') %>
          <%= link_to '', role, method: :delete, data: { confirm: 'Are you sure?' }, :class => "delete" %>
        </td>
      </tr>
    <% end %>
  </table>
</div>
